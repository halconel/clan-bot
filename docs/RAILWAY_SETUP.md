# Руководство по настройке Railway.app для деплоя бота

Эта инструкция поможет вам задеплоить Telegram бота на Railway.app с нуля.

## Шаг 1: Регистрация на Railway.app

1. Перейдите на https://railway.app
2. Нажмите **"Start a New Project"** или **"Login"**
3. Выберите **"Login with GitHub"**
4. Авторизуйте Railway доступ к вашему GitHub аккаунту
5. После регистрации вы получите **$5 бесплатных кредитов** (~500 часов работы)

## Шаг 2: Создание нового проекта

1. На главной странице Railway нажмите **"New Project"**
2. Выберите **"Deploy from GitHub repo"**
3. Railway попросит разрешение на доступ к вашим репозиториям:
   - Нажмите **"Configure GitHub App"**
   - Выберите **"Only select repositories"**
   - Выберите репозиторий `clan-bot`
   - Нажмите **"Save"**
4. Вернитесь в Railway и выберите репозиторий `clan-bot`
5. Railway автоматически определит, что проект использует Docker

## Шаг 3: Настройка Deploy Branch (ВАЖНО!)

По умолчанию Railway может деплоить из любой ветки. Нужно ограничить деплой только веткой `main`.

1. В проекте Railway перейдите в **Settings** (иконка шестеренки)
2. Найдите секцию **"Source"**
3. В поле **"Deploy Branch"** выберите или введите `main`
4. Убедитесь, что **"Auto-Deploy"** включен (галочка)
5. Нажмите **"Save"**

Теперь Railway будет автоматически деплоить только при push в ветку `main`.

## Шаг 4: Настройка переменных окружения

### 4.1. Получить BOT_TOKEN

1. Откройте Telegram
2. Найдите бота **@BotFather**
3. Отправьте команду `/newbot`
4. Следуйте инструкциям:
   - Введите имя бота (например, "Kingdom Clash Bot")
   - Введите username бота (должен заканчиваться на `bot`, например `kingdom_clash_bot`)
5. Скопируйте токен (выглядит как `1234567890:ABCdefGHIjklMNOpqrsTUVwxyz`)

### 4.2. Получить LEADER_TELEGRAM_ID

1. Откройте Telegram
2. Найдите бота **@userinfobot**
3. Отправьте ему любое сообщение
4. Бот ответит вашим Telegram ID (например, `123456789`)
5. Скопируйте этот ID

### 4.3. Настроить Google Sheets

Следуйте инструкции из `docs/GOOGLE_SHEETS_SETUP.md` (будет создана позже) или:

1. Перейдите на https://console.cloud.google.com/
2. Создайте новый проект
3. Включите Google Sheets API
4. Создайте Service Account
5. Скачайте JSON ключ (credentials.json)
6. Создайте Google Sheets таблицу
7. Дайте доступ Service Account к таблице (по email из credentials.json)

### 4.4. Добавить переменные в Railway

1. В проекте Railway перейдите в **Variables** (иконка переменных)
2. Нажмите **"New Variable"**
3. Добавьте следующие переменные:

| Variable Name | Value | Где взять |
|--------------|-------|-----------|
| `BOT_TOKEN` | `1234567890:ABC...` | От @BotFather |
| `LEADER_TELEGRAM_ID` | `123456789` | От @userinfobot |
| `SPREADSHEET_ID` | `1a2b3c4d5e...` | Из URL Google Sheets |
| `SHEET_NAME` | `Игроки` | Название листа в таблице |
| `GOOGLE_CREDENTIALS` | `{"type":"service_account",...}` | Содержимое credentials.json |
| `SCREENSHOTS_DIR` | `data/screenshots` | Оставить как есть |
| `TEMP_STORAGE_FILE` | `data/pending.json` | Оставить как есть |
| `LOG_LEVEL` | `INFO` | Оставить как есть |

**Важно для GOOGLE_CREDENTIALS:**
- Откройте файл `credentials.json` в текстовом редакторе
- Скопируйте **ВСЕ** содержимое (включая фигурные скобки)
- Вставьте в поле Value как одну строку
- Пример: `{"type":"service_account","project_id":"my-project",...}`

## Шаг 5: Настройка Volume для данных

Volume нужен для сохранения screenshots и pending.json между перезапусками.

1. В проекте Railway перейдите в **Settings**
2. Найдите секцию **"Volumes"**
3. Нажмите **"Add Volume"**
4. Заполните:
   - **Mount Path:** `/app/data`
   - **Name:** можно оставить автоматическое
5. Нажмите **"Add"**

## Шаг 6: Первый деплой

После настройки всех переменных и Volume:

1. Railway автоматически начнет деплой
2. Следите за процессом в разделе **"Deployments"**
3. Процесс деплоя:
   - Build: Railway собирает Docker образ из Dockerfile
   - Deploy: Railway запускает контейнер
4. Когда статус станет **"Success"**, бот запущен!

### Проверка логов

1. Перейдите в раздел **"Logs"** (иконка документа)
2. Вы должны увидеть логи запуска бота
3. Если есть ошибки, они будут здесь

### Тестирование бота

1. Откройте Telegram
2. Найдите вашего бота по username
3. Отправьте `/start`
4. Бот должен ответить приветственным сообщением

## Шаг 7: Настройка защиты main ветки в GitHub

Чтобы случайно не задеплоить неттестированный код:

1. Перейдите в GitHub репозиторий `clan-bot`
2. Settings → Branches
3. Нажмите **"Add rule"**
4. **Branch name pattern:** `main`
5. Включите:
   - ✅ Require a pull request before merging
   - ✅ Require status checks to pass before merging
   - ✅ Require branches to be up to date before merging
6. В **"Status checks"** выберите `test` (появится после первого PR)
7. Нажмите **"Create"**

Теперь все изменения в main будут проходить через PR и тесты!

## Последующие деплои

После первой настройки процесс очень простой:

1. Создаете feature ветку: `git checkout -b feature/my-feature`
2. Делаете изменения, коммитите
3. Push: `git push -u origin feature/my-feature`
4. Создаете Pull Request в main
5. CI автоматически запускает тесты
6. После прохождения тестов → Merge PR
7. **Railway автоматически задеплоит новую версию!**

Никаких ручных действий не требуется.

## Мониторинг

### Логи в реальном времени

Railway → Logs → выбрать ваш проект

### Метрики

Railway → Metrics → CPU, Memory, Network

### Алерты

Settings → Notifications → можно настроить уведомления в Telegram при падении бота

## Rollback (откат изменений)

Если что-то пошло не так:

### Вариант 1: Через Railway (быстро)

1. Railway → Deployments
2. Найдите предыдущий успешный деплой
3. Нажмите меню (три точки) → **"Rollback"**
4. Бот вернется к предыдущей версии

### Вариант 2: Через Git

```bash
git checkout main
git revert HEAD  # Откатить последний коммит
git push
```

Railway автоматически задеплоит откат.

## Troubleshooting (Решение проблем)

### Бот не запускается

1. Проверьте логи в Railway
2. Частые причины:
   - Неверный `BOT_TOKEN`
   - Неверный формат `GOOGLE_CREDENTIALS`
   - Нет доступа Service Account к Google Sheets

### "Build failed"

1. Проверьте, что Dockerfile существует
2. Проверьте, что requirements.txt корректен
3. Посмотрите логи build в Railway

### Бот не отвечает

1. Проверьте, что бот запущен (Railway → Status)
2. Проверьте логи на ошибки
3. Проверьте переменные окружения

### Превышен лимит часов

Railway дает 500 часов/месяц бесплатно (~20 дней непрерывной работы).

**Решение:**
- Добавить кредитную карту для $5/месяц (unlimited hours)
- Или переключиться на Oracle Cloud Free Tier (навсегда бесплатно)

## Полезные ссылки

- [Railway Documentation](https://docs.railway.app/)
- [Railway Discord](https://discord.gg/railway) - поддержка сообщества
- [Railway Status](https://status.railway.app/) - статус сервиса

## Следующие шаги

После успешного деплоя:

1. Протестируйте все функции бота
2. Настройте Google Sheets таблицу
3. Добавьте бота в группу Telegram (если планируется)
4. Настройте регулярные бэкапы данных

Поздравляю! Ваш бот задеплоен и работает 24/7! 🎉
